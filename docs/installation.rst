============
Installation
============

At the command line::

    $ easy_install sampler_survey

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv sampler_survey
    $ pip install sampler_survey
